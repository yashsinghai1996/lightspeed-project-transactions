# lightspeed-project-transactions
dbt project for transaction lifecycle modeling with bronze, silver, and gold layers, analytics marts, tests, and snapshots

This repository uses GitHub Actions for basic dbt pull request validation.
It also supports optional CI execution of `dbt build` when Databricks secrets are configured in GitHub.

This repository includes GitHub Actions CI for dbt project validation on pull requests.
